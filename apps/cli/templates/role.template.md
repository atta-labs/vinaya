---
role_id: {{ROLE_ID}}
title: {{ROLE_ID}}
order: 99
description: Replace this with a one-sentence description of what {{ROLE_ID}} does.
actor: agent
performs:
  - replace-this-with-what-{{ROLE_ID}}-does
refuses_when: >
  Replace this with the conditions under which {{ROLE_ID}} refuses to act.
summary: Replace this with one memorable line about {{ROLE_ID}}.
---
# {{ROLE_ID}} — Role Reference

## The short version

Replace this section with what {{ROLE_ID}} owns, does, and refuses to do.
`vinaya check --plan` validates this contract's structural shape only — the
frontmatter keys above, and this section being non-empty — never the prose
itself.
