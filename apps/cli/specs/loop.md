# The dev-review-loop — command, rounds, exits, publication and pause

Status: draft

`vinaya dev-review-loop --task <n> --agent <claude|codex|gemini>` (`apps/cli/src/commands/dev-review-loop.ts`, `devReviewLoopCommand`) runs the developer/reviewer round loop for one task Issue, unattended, until it publishes or pauses. The full design is the Linear "Tech spec — Developer Review Loop" (rev 4); this file is the durable, in-repo reference for what shipped across `dev-review-loop-v1` tasks 4–6 — the state machine, the driver, publication and pause — not a restatement of the whole spec.

`packages/aeg-core/src/dev-review-loop/` is the policy layer: `assessRound` (task 4, `#414`) is the ENTIRE decision logic — every stop condition, every confidence rule, the `Decision` a round produces — as one pure function, no `fs`/`fetch`/`process.env`/subprocess/prompt anywhere in that directory. `apps/cli/src/lib/dev-review-loop.ts` (`devReviewLoop`, tasks 5–6) is the driver: it turns real forge/dispatch facts into `Observations`, calls `assessRound`, and acts on the `Decision` it returns. It never re-implements a stop condition or a confidence rule.

## The command

```
vinaya dev-review-loop --task <n> --agent claude|codex|gemini [--json]
vinaya dev-review-loop --resume <pr> --agent claude|codex|gemini [--json]
```

`--task <n>` starts a fresh loop against task Issue `<n>` — `n` is a GitHub Issue number, not a tranche task ordinal. `--resume <pr>` continues a previously paused loop from its held state (see "Pause and `--resume`" below); it takes a pull-request number, since a paused loop is anchored to an already-open PR, not an Issue. `--agent` falls back to `dispatch.agent` in `vinaya.config.json` when omitted. Both forms build the same `LoopInput` union and make exactly one call into `devReviewLoop` — `apps/cli/specs/surface.md`'s one-command-one-effects-call rule.

## Rounds

Round 1's entry (`review-validity-v1` task 5, `#488`, O4) checks the forge before dispatching anything: an already-open pull request on the developer's branch means ATTACH — no developer is dispatched at all, the recorded session (`readResumeRecord`, `dispatch.ts`'s own durable `~/.vinaya/dispatch-resume/<repo>/developer-<agent>-issue<n>.json`) is read so a later round resumes it, and round 1 enters its gate directly. A branch that exists on the remote with no open PR yet resumes that same recorded session ONCE, instructed to open the PR through the validated path, and waits for it. Neither case starts a fresh developer. Only when the branch does not exist remotely at all does round 1 dispatch the developer fresh, brief read verbatim from the task Issue's frozen brief comment (`fetchFrozenBrief`), then wait for the PR it opens. `fetchFrozenBrief`, and every other reader of "the frozen brief" in this codebase, resolve through `@attalabs/aeg-core`'s `resolveNewestFrozenBrief` (task-run-v1 task 4, `#483`, O3): a Principal can correct a wrong frozen brief with `vinaya task brief <tranche> <n> --supersede --reason <text>`, which appends a new, higher-versioned `aeg:brief:v<k>` comment naming its predecessor and the reason rather than editing or deleting the original — the loop always reads the newest version, never the first one posted.

1. **Gate.** `resolveHead`/`fetchCiConclusion` read the branch's live head and CI conclusion — never run locally (Traps: the mechanical gate is the CI system's own verdict, not a local re-run). `fetchCiConclusion` excludes the review gate's own check-run by name (`REVIEW_GATE_CHECK_RUN_NAME`, `review-gate-check-name.ts` — the one constant `check-review-gate.ts` also imports, never a second copy; `review-validity-v1` task 5, `#488`, O1) in every status, so a head with green CI and no verdicts yet reads green, never red because the gate itself hasn't posted one. Red sends the developer back for the same round, naming the failing check-run(s) (`fetchFailingCheckNames`, O3) — never the review gate, which is excluded entirely, and never a bare "CI is red." After that dispatch the driver waits for the branch head to change (the same gate poll budget, `gatePollMaxAttempts`/`gatePollIntervalMs`) before reading the gate again — it never re-reads a still-unchanged head in a tight loop (`#479`: five re-dispatches in two minutes on one head). A developer turn that produces no push at all is one strike against a small, driver-owned bound (`MAX_GATE_STALLED_TURNS`, `dev-review-loop.ts`); consecutive strikes on one head that reach the bound end in `pause{reason:'infrastructure'}` naming the head and the failing check (never unbounded re-dispatch) — see "Driver-decided pauses," below, for why this reuses `'infrastructure'` rather than a dedicated reason. Green and round 1 goes straight to reviewers; green from round 2 on gates on developer confidence first (below).
2. **Reviewers.** Both roles are dispatched fresh every round (`dispatchRole` called anew — a reviewer session is never resumed; only the developer's is). Each reads facts-only (`renderReviewerPrompt`: objectives, Principal rulings, head, CI conclusion, the frozen brief's own source revision (`fetchSourceRevision` — task 4, `#483`, O2; the `NO_SOURCE_REVISION` sentinel on a brief from before this task that carries no `**Revision:**` line) — never developer- or PR-body-authored prose, `lintReviewerPrompt` enforces this) and writes `findings.txt`/`report.txt` to a driver-chosen work directory, plus `objectives.txt` whenever the task carries objectives (the prompt names the file only then; a task with no `## Objectives` section never asks for it). `resolveIssueObjectives` (`review-validity-v1` task 2, `#476`, O1) is the one source of the `OBJECTIVES:` fact and the version threaded through the round: the newest principal-authored `<!-- aeg:objectives:v<k> -->` edit comment when one exists — its own `Version:` line is read as-is, never recomputed, since `vinaya issue objectives edit` already wrote it from the exact list it spliced into the live Issue body — else the principal-authored frozen brief's verbatim copy of the Issue's `## Objectives` section, versioned by the same `objectivesOf`/`objectivesVersion` pair the merge gate calls on its own live-body read. Never the live Issue body directly (PR #445's security round: any collaborator can edit a body and thereby author every reviewer's prompt). `buildVerdictFromReport` derives the verdict from findings (never hand-typed) and renders the same `VERDICT:`/`Judged head:`/`Objectives version:`/`Ruling ordinal:` shape `review-post.ts` uses elsewhere, carrying the version and ruling ordinal the round was dispatched against and one `O<n>: MET | NOT MET` line per objective; `writeHeldVerdict` holds it in the outbox — nothing is posted to the forge before green (Section 10 stop condition, tasks 4–5). A work directory still missing `findings.txt`/`report.txt` (or `objectives.txt` on a task that carries objectives) after the dispatch is an infrastructure outcome, never a clean verdict — an empty `findings.txt` is still clean, exactly as the reviewer contract promises (`review-validity-v1` task 1, `#475`, O1); see "Infrastructure failures," below.

   Right after both reviewers return — before either verdict is written to the outbox — the driver re-resolves objectives one more time and compares the version to the one the round was dispatched against (`review-validity-v1` task 2, `#476`, O3). A mismatch means a principal posted an objectives edit while this round's reviewers were running: both verdicts are discarded in memory, never held and never published, and the round ends in `pause{reason:'objectives_changed'}` (below) rather than judging the round against a list the reviewers never actually saw. The driver does the identical re-check for the newest principal ruling ordinal (`review-validity-v1` task 3, `#477`, O3): a mismatch there ends the round in `pause{reason:'ruling_posted'}` instead, for the identical reason — a ruling the reviewers never saw must not be judged against.
3. **Verdicts.** `assessRound` combines both verdicts. Clean (both APPROVE/PASS, every objective MET) → `publish`. An `ESCALATE` from either role → `pause{reason:'escalation'}`, immediately, before any verdict is ever held or posted. Otherwise the findings are relayed to the developer for another round.

Round numbering: `round` advances only when a genuine review round concludes `changes_requested` and hands back another developer dispatch. A mechanical-gate-red retry and the confidence rule's one-extra-turn both resubmit the same round number — advancing on every developer dispatch would make a CI hiccup on round 1 silently trigger the round-1-never-asks confidence question.

## The confidence rule

Round 1 never asks for confidence. From round 2 on, a green gate reads `.vinaya-confidence` (written by the developer per `CONFIDENCE_PROMPT_LINE`'s instruction) before reviewers are ever dispatched:

- absent, asked once already → `pause{reason:'confidence'}`; absent, first time → re-ask once.
- below 50, one extra turn not yet spent → one more developer turn (spent once, ever), no reviewer dispatch; below 50 again → `pause{reason:'confidence'}`.
- 50 or over → dispatch reviewers.

## The four exits

`assessRound` decides every exit — the driver never re-derives one:

| Exit | Trigger | `Decision` |
| --- | --- | --- |
| green | both verdicts clean, every objective MET | `publish` |
| stalled | a fingerprint resolved earlier reappears (`reappearance`); two consecutive rounds resolve nothing (`no_progress`) | `pause` |
| capped | round count exceeds `MAX_ROUNDS` (3) | `pause{reason:'max_rounds'}` |
| escalated | either reviewer returns `ESCALATE` | `pause{reason:'escalation'}` |

The confidence rule's own collapse (`pause{reason:'confidence'}`) is a fifth practical pause path layered on top of "stalled," gating whether a round even reaches a verdict.

### Infrastructure failures (O1/O2, `review-validity-v1` task 1, `#475`) and driver-decided pauses (`review-validity-v1` task 5, `#488`, O2/O5)

A review role's dispatch is retried once, into a genuinely fresh work directory (never the failed attempt's own), if its directory is still missing a required artifact after the first attempt; a second miss on the same role, and a red-gate developer turn that produces no push `MAX_GATE_STALLED_TURNS` times in a row on one head, are both `pause{reason:'infrastructure'}` — the driver's own two "the loop's own mechanics stalled, not a review verdict" exits, distinguished from each other only by the pause's `detail` text (the role and missing artifact(s), or the head and the failing check-run(s)).

Neither corresponds to an `Observations` kind `assessRound` accepts, and this task's own declared Surface (Issue #488 §4) excludes `packages/aeg-core` — adding one, or a dedicated `PauseReason`, would mean editing it. `driverDecidedPauseEvents` (`dev-review-loop.ts`) instead hand-builds the exact same `stop_condition_met`/`paused`/`round_ended`/`journal_finalized` events every policy-decided pause gets, reusing the schema's existing, otherwise-unused `'principal_stop'` condition and generic `'principal_item'` paused-reason — never a new schema value — so a reader (or a machine parsing the outbox) sees a real, complete pause record either way. This closes a regression (PR #489 round 2, MAJOR): the reviewer-infrastructure pause used to be built by the driver's own hand-rolled `Decision`, skipping the log entirely. No verdict is held, none is published, and the round number does not advance (the same way a mechanical-gate-red retry never advances it). The pause comment names the failing role and the missing artifact(s), or the head and the failing check-run(s), it observed.

### Objectives changed mid-round (`review-validity-v1` task 2, `#476`, O3)

`'objectives_changed'` is a real `PauseReason` member (`packages/aeg-core/src/dev-review-loop/types.ts`) — this task's own declared Surface includes that package, unlike the driver-decided pauses above. The driver still decides it itself, the same way it decides `'infrastructure'`: comparing two `resolveIssueObjectives` reads (the one taken before dispatching this round's reviewers, the one taken right after both return) is not something `assessRound` can do from an `Observations` value, so the comparison lives in `dev-review-loop.ts`, and `driverDecidedPauseEvents` logs it through the same reused event shape. The pause's `detail` names the version the round was dispatched against, the version now current, and the exact `vinaya issue objectives edit` invocation that moved it — reconstructed by `describeObjectivesEdit` from the edit-audit comment's own `Previous:`/`Now:`/`Reason:` lines, never executed. As with the infrastructure pauses above, no verdict is held and none is published for that round, and the round number does not advance.

### Ruling posted mid-round (`review-validity-v1` task 3, `#477`, O3)

`'ruling_posted'` is the ruling-ordinal twin of `'objectives_changed'` above, same `PauseReason` member shape, same detection site, same reason for living in `dev-review-loop.ts` rather than `assessRound`: the driver compares the newest principal ruling ordinal it resolved when it dispatched this round's reviewers (`fetchNewestRulingOrdinal`, captured into `facts.rulingOrdinal`) against the ordinal it resolves right after both return, before either verdict is held. A mismatch means a principal posted a ruling while this round's reviewers were running — that round's verdicts are discarded in memory the same way (never held, never published), and the round ends in `pause{reason:'ruling_posted'}`, its `detail` naming the ordinal the round was dispatched against, the ordinal now current, and the ruling's own marker identifier (`<pr>-<k>`). No reconstructed command here — unlike an objectives edit, a ruling carries no replayable invocation, only its identifier and content. The next round's reviewers see the new ruling automatically: `dispatch_reviewers` re-resolves both `rulings` and `rulingOrdinal` fresh at the top of every round, mid-round check included.

## Publication (O1, task 6)

At `publish`, `publishRound` (`apps/cli/src/lib/dev-review-loop.ts`) posts, through this repo's `gh pr comment` forge-write mechanics, in order: the round's held reviewer verdict, the held security verdict, then one summary comment from `renderSummary` (`@attalabs/aeg-core`) built from the loop's own round records. Each verdict post is re-read straight back off the PR and re-parsed through `extractCodeReviewVerdict`/`extractSecurityReviewVerdict` — the SAME extractors the merge gate itself calls — and refused (thrown) if it does not resolve cleanly to the round's judged head. The summary is checked BEFORE it is posted: both extractors run over the rendered text, and a summary that resolves as a real verdict through either one is refused rather than posted (`renderSummary`'s own contract is no `VERDICT:`/`Judged head:`/`Objectives version:`/`Ruling ordinal:` line; this is re-verified live rather than trusted).

Idempotency: each of the three posts is wrapped in `postForgeEffectOnce`, which records an effect id in the outbox as `started` before the `gh` call and rewrites it `posted` (with the returned URL) right after — mirroring `dispatch.ts`'s own effect-id-before/checked-after discipline, adapted to a comment post since `DevReviewLoopEvent` carries no `effect_id` field of its own. A rerun that finds an already-`posted` record for a given post returns its recorded URL without calling `gh` again — nothing is posted twice.

Supersession (editing a posted verdict, or marking one superseded after a later push) is out of `dev-review-loop-v1`'s scope: nothing is ever posted before green, so there is nothing to supersede in v1.

## Pause and `--resume` (O2, task 6)

At `pause{reason}`, the driver writes the round's held state (task, round, judged head, branch, PR number, reason, and — for `'infrastructure'` (both the reviewer case and the gate-stalled-head case; see above), `'objectives_changed'` (the old version, the new version, and the superseding command; see above), or `'ruling_posted'` (the old ordinal, the new ordinal, and the ruling's marker identifier; see above) — the detail it observed) to the outbox, then posts one comment on the PR marked `<!-- aeg:loop:paused:<reason> -->`, carrying the reason (plus that same detail, where one exists) and the exact resume command (`vinaya dev-review-loop --resume <pr>`, the real PR number substituted in). The pause comment carries no verdict grammar. The CLI command exits non-zero — the process-level signal an unattended dispatcher watches for.

`--resume <pr>`:

1. Reads the PR's body for its `Closes #N` reference to find the task.
2. Reads that task's held pause state from the outbox; refuses if none exists.
3. Requires a Principal-authored ruling comment on the PR (`fetchRulings`, non-empty) — refuses if none exists yet.
4. Re-resolves the PR branch's live head and compares it to the held state's recorded head. A mismatch refuses, naming the round to restart from — resume never silently restarts from round 1.
5. Re-enters the round loop at the held round, dispatching the developer fresh (a paused process's vendor session id is not durable across a restart — `DispatchOutcome` never persists one) with the Principal's ruling text as context instead of reviewer findings.

**v1 accepted loss:** resume re-enters with a fresh `LoopState` at the held round number — the round number itself governs the confidence rule's round ≥ 2 branching correctly, but the fingerprint/finding-identity memory (`lastIds`, `previousResolvedEmpty`) a full crash-recoverable journal would carry across the pause boundary does not persist. A stall trigger that depends on cross-round finding identity (reappearance, no-progress) resets at a resume. No infrastructure exists yet to serialize `LoopState`'s `Map`-typed fields across a process restart; building it is out of this task's boundary.

## Doc-owners binding

`.vinaya/doc-owners` binds `apps/cli/src/lib/dev-review-loop.ts` to this file — a code change to the driver requires this file to appear in the same diff (or a `Doc-ack`/waiver), per `verify-docs` C5.
