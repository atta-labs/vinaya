**Boundary:** Ship the tranche-slug/`D-###` citation ban as a genuinely pluggable Vinaya check — regex patterns + scope configured per-consumer via `vinaya.config.json`, not hardcoded in attalabs' internal `packages/aeg-core/src/retired-vocabulary.test.ts` — and use attalabs' own configuration of it to sweep Vāda's, Herald's, and attalabs' own remaining doctrine surfaces (`specs/**`, `CLAUDE.md` files — never `src`/`tests`/fixtures) clean of the same citation class #729/#734 already cleaned from Vinaya's and `.claude/skills/**`'s.

**Sizing:** Two real parts, likely needs splitting at brief time: (1) the generic pluggable check itself (regex-configurable patterns + scope, shipped as a real `CheckSpec`/`vinaya.config.json`-driven primitive, with attalabs configuring its own instance for its own surfaces), and (2) the citation-strip sweep across Vāda/Herald/attalabs' doctrine files once that instance is running against them. Large.

**Project(s) + blast radius:** Project: attalabs, vinaya, aeg-core, vada, herald. The check primitive itself ships from `packages/aeg-core` or `apps/vinaya/cli` (TBD at brief time); Vāda's and Herald's own `specs/**`/`CLAUDE.md` doctrine files get swept for citations (no code/logic touched in either product). Broad — touches a `.aeg/packages`-domain surface (whichever package the check ships from) and multiple products' doc trees; every project whose files this task edits is listed above, so no `blast-radius-ack` needed beyond that enumeration.

**Dependency rationale:** Builds directly on #729/#734 (proved the pattern+scope design and the false-positive triage process on Vinaya's own surfaces) and mirrors #730's stated goal (a generic primitive shipped in `@atta/aeg-core`/Vinaya form, with attalabs configuring its own instance via `vinaya.config.json` — the same "attalabs is just another consumer" proof this session has been building toward). Related to #731 (moving `reader-resolvable-prose` out of the hardcoded core registry into opt-in config) — same architectural direction: this check should stop being an attalabs-only `vitest` file no adopter can install, and become something an adopter (including attalabs itself) opts into and configures.

**Traps to avoid:** Do NOT blind-widen scope to match-everything again — proven costly in #734/PR#735's own history (`['.']` produced ~600 false positives before triage). Each new product surface needs the same manual triage already done for Vinaya: legitimate test fixtures using real tranche names as data (exclude `src`/`tests`/fixtures entirely, doctrine/spec/`CLAUDE.md` surfaces only), and a product's own unrelated `-vN` naming convention (Vāda's historical `crucible-v1.yaml` already tripped this once). Do NOT conflate "ship a generic pluggable check" with "delete attalabs' internal `retired-vocabulary.test.ts`" — whether the internal test stays as attalabs' own house-style gate alongside a new shipped check, or gets replaced entirely by attalabs configuring the shipped check against itself, is a real architecture decision. Escalate to the Principal if ambiguous rather than deciding silently — this session already had one incident of a unilateral scope decision that needed after-the-fact ratification; route this one through a real question instead.

**Suggested agent-class:** Developer-tier; Planner should consider splitting into 2+ tasks (primitive + config wiring, then per-product sweep) at brief-authoring time given the size.

**Stop-and-escalate:** If the "shipped CheckSpec vs. attalabs-internal vitest file" question isn't resolved by rereading #730/#731's own rationale first, stop and ask the Principal rather than guess. If a product's spec surface produces a false-positive ratio anywhere near Vinaya's (needing many exemptions), report the ratio and ask before exempting broadly.

**Docs to keep coherent:** `packages/aeg-core/src/retired-vocabulary.test.ts`'s own header docstring (its `PRODUCT`/`PATTERN_SCOPE` design explanation needs updating if the architecture changes); `apps/vinaya/cli/src/lib/config.ts` (if `vinaya.config.json`'s `checks`/built-ins schema needs a new entry); #732's canonical "what ships as Vinaya vs what's attalabs' own" doc — this task is a direct test case for that doc's own framing and should be written with #732 open alongside it.
